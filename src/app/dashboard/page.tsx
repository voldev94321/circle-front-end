'use client';

import RestrictedProvider from "@/providers/RestrictedProvider";

const Dashboard = () => {
    return (
        <RestrictedProvider>
            asdf
        </RestrictedProvider>
    );
};

export default Dashboard;